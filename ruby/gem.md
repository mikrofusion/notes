

```
gem install gem-open
gem open sass
```

install rdocs with
------------------
```
gem rdoc sass
gem server
```

when using rvm, can type rvm ```info```.
gem source code will be in 

    ```
    cd $GEM_HOME
    cd gems/<gem>
    ```